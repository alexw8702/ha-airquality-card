{
  "name": "ha-airquality-card",
  "private": true,
  "description": "Air Quality Card für Home Assistant — Build- und Test-Tooling (die ausgelieferte Karte in dist/ bleibt ein einzelnes, direkt ladbares Skript ohne Laufzeit-Dependencies)",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "npm run build && node --test"
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  }
}
