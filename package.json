{
  "name": "ha-airquality-card",
  "private": true,
  "description": "Test tooling for the Air Quality Card (the shipped card itself has no build step)",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  }
}
